spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${ GOOGLE_ID }
            client-secret: ${ GOOGLE_SECRET }
            redirect-uri: ${ GOOGLE_URL }
            authorization-grant-type: authorization_code
            scope:
              - email
              - profile