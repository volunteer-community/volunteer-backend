spring:
  profiles:
    include:
      - db
      - aws

  # ??? swagger ??? ?? ?? ????.
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

  # ???? update? ?? ????
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    show-sql: true

